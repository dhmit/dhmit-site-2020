{%- capture titleHtml -%}
  <h2 class="{% classNames 'section-title f18 s:f21 lh119 ls02em', cx %}">{{ title }}</h2>
{%- endcapture -%}

{% if viewMoreHref %}
  <div class="df jcb aic">
    {{ titleHtml }}
    <a class="mono f18 lh100 dn m:db" href="{{ viewMoreHref }}">{{ viewMoreText | default: 'View more' }} →</a>
  </div>
{% else %}
  {{ titleHtml }}
{% endif %}


{% assign viewMoreHref = blank %}
{% assign viewMoreText = blank %}